## Description
Provide a brief description of the changes.

## Verification
- [ ] Code is formatted and linted (`uv run ruff check .` and `uv run ruff format --check .`)
- [ ] Tests pass locally (`uv run pytest -q`)
- [ ] Type checking passes (`uv run mypy src tests`)
- [ ] No real secrets or sensitive data are included.

## Security Considerations
If this PR modifies security controls or architecture, please detail the changes and refer to the threat model.
